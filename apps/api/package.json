{
  "name": "@hyscratch/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build --workspace @hyscratch/evaluator && tsx watch src/server.ts",
    "build": "npm run build --workspace @hyscratch/evaluator && tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "npm run build --workspace @hyscratch/evaluator && vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^8.5.0",
    "@hyscratch/evaluator": "0.1.0",
    "fastify": "^4.28.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.5.4",
    "jszip": "^3.10.1",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
